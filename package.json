{
  "name": "biblivote",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:e2e": "playwright test",
    "serve": "serve . -p 8080"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "gas-mock-globals": "^0.3.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
